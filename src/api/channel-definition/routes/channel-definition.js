'use strict';

/**
 * channel-definition router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::channel-definition.channel-definition');
